interface:
  display_name: "Product Manager"
  short_description: "Clarify product decisions without turning every request into a PRD."
  default_prompt: "Use $product-manager to identify the product decision, choose the lightest planning mode, and define the smallest scope that can be accepted."

policy:
  allow_implicit_invocation: true

